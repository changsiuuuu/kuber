apiVersion: v1
kind: ConfigMap
metadata:
  name: step6-scope-trap
data:
  {{- range .Values.subdomains }}
  domain-{{ . }}: "{{ . }}.{{ $.Values.globalSuffix }}.com"
  chart-name-{{ . }}: "{{ $.Chart.Name }}"
  {{- end }}